# 🧴 Bottle Detection with YOLOv5 and OpenCV

Проект на Python для обнаружения бутылок в реальном времени с использованием веб-камеры, модели YOLOv5 и OpenCV.

Установка:

1. Клонируйте репозиторий YOLOv5 и перейдите в папку:
git clone https://github.com/ultralytics/yolov5
cd yolov5

2. Создайте виртуальное окружение и активируйте его (рекомендуется):
python3 -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate

3. Установите зависимости:
pip install -r requirements.txt
pip install opencv-python

4. Убедитесь, что установлен PyTorch. Если нет, посетите https://pytorch.org/get-started/locally и выберите команду установки под вашу систему.

Запуск:

1. Поместите файл main.py в корень проекта (рядом с папкой yolov5).

2. Запустите скрипт командой:
python main.py

3. Откроется окно с видео с камеры и обнаружением бутылок. Для выхода нажмите q.